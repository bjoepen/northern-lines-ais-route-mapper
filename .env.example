# Northern Lines AIS Route Mapper
#
# Build 001 does not require application secrets or API keys.
# Add documented runtime configuration here only when a future integration
# introduces an explicit environment contract.
